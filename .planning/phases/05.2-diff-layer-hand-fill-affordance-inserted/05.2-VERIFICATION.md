---
phase: 05.2-diff-layer-hand-fill-affordance-inserted
verified: 2026-06-25T00:00:00Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
mode: mvp
requirements_verified:
  - HANDFILL-01
commits_verified:
  - 3e84bec  # test(05.2-01) RED
  - 98c265e  # feat(05.2-01) GREEN
  - 4b6ce4a  # docs(05.2) REVIEW
  - 9a62c1a  # test(05.2-02) RED follow-up (CR-01 + WR-01)
  - 01d7d1e  # fix(05.2-02) GREEN follow-up (two-phase + D-60 scope)
  - cdc2b97  # docs(05.2) REVIEW resolution update
---

# Phase 5.2: Diff-Layer Hand-Fill Affordance — Verification Report

**Phase Goal (User Story):**
> **As a** submitter who hand-fills a value into the auto-generated `systemname.yaml`
> for a field the collector returned `""` for on the previous run, **I want to** be
> able to re-run the benchmark against the same results-dir WITHOUT triggering
> `SystemDriftError E404`, **so that** I can enrich the auto-detected client identity
> with human-known context while still being protected against real hardware drift.

**Verified:** 2026-06-25
**Status:** passed
**Mode:** mvp
**Re-verification:** No — initial verification of the FINAL state after CR-01/WR-01 follow-up cycle.

## Goal Achievement (User Flow Coverage — MVP Mode)

The phase goal is a User Story. The `so that` outcome clauses are:

1. Re-run benchmark against same results-dir does NOT trigger `SystemDriftError E404` when submitter has hand-filled a scalar fingerprint position that the collector returns `""` for.
2. Enrich auto-detected client identity with human-known context (model nicknames, chassis models) — i.e., hand-fills SURVIVE the re-run.
3. Still protected against real hardware drift — collector returning a NEW, non-empty value that disagrees with the hand-fill STILL raises drift.

### User Flow Coverage

| Step | Expected | Evidence | Status |
| ---- | -------- | -------- | ------ |
| Submitter hand-fills `chassis.model_name` in YAML; re-runs with collector returning `""` | `DiffResult.empty is True`; no drift raised | `test_handfilled_chassis_model_name_survives_rerun_with_empty_collector` PASSED (test_diff.py:470) | ✓ VERIFIED |
| Same for `chassis.cpu_model` | `DiffResult.empty is True` | `test_handfilled_cpu_model_survives_rerun_with_empty_collector` PASSED (test_diff.py:481) | ✓ VERIFIED |
| Same for `os_name` + `os_version` | `DiffResult.empty is True` | `test_handfilled_os_name_survives_rerun_with_empty_collector` PASSED (test_diff.py:490) | ✓ VERIFIED |
| Real drift: collector returns DIFFERENT non-empty value for a hand-filled field | `DiffResult.empty is False`; drift raised | `test_real_drift_handfilled_position_recomputed_to_different_non_empty_still_raises` PASSED (test_diff.py:502) | ✓ VERIFIED |
| Non-fingerprint observable (sysctl) drift still raises | `DiffResult.empty is False` | `test_real_drift_non_handfill_collector_change_still_raises` PASSED (test_diff.py:515) | ✓ VERIFIED |
| Collector finally learns a value the user did NOT hand-fill (reverse direction D-60) | INFO log emitted; no DiffEntry; no drift | `test_reverse_direction_collector_resolves_value_emits_info_log_no_drift` + `test_reverse_direction_info_log_includes_field_path_and_resolved_value` PASSED (test_diff.py:591, 610) | ✓ VERIFIED |
| Multi-client topology with one hand-fill pairs correctly | Unique soft-pair candidate pairs; no conflation | `test_multi_client_topology_two_handfilled_three_total_pairs_correctly` PASSED (test_diff.py:644) | ✓ VERIFIED |
| Ambiguous soft-pair candidates fall back to orphan emission | `DiffResult.empty is False`; fingerprint-level entries surface | `test_ambiguous_soft_pair_two_candidates_falls_back_to_orphans` PASSED (test_diff.py:530) | ✓ VERIFIED |

The user-story outcome is OBSERVABLY TRUE in the codebase: each step of the user flow is locked by a passing test that exercises the public `diff_node_dict_lists` API.

## Observable Truths (Phase 5.2 ROADMAP Success Criteria + D-60..D-63)

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| SC#1 | `_compute_fingerprint(stanza)` semantics unchanged (D-38 11-tuple) | ✓ VERIFIED | `_compute_fingerprint` body at `diff.py:230-238` matches pre-5.2 implementation byte-for-byte; existing `TestRoundTripEqualIsEmpty` (3 tests) + `TestFlattenToPaths` (5 tests) + `TestFieldChangeIsDrift` (4 tests) all PASS |
| SC#2 | Two-pass pairing: exact-match (pass-1), then soft-pair (pass-2) | ✓ VERIFIED | `diff_node_dict_lists` at `diff.py:487-551` shows explicit three-pass flow: common_fps (pass 1) → soft-pair (pass 2 via `_soft_pair_orphans`) → orphan emission (pass 3a/3b); `test_exact_match_pass_still_preserves_identical_fingerprints_no_soft_pair_invoked` + 3 hand-fill survival tests all PASS |
| SC#3 | Soft-paired clients fall through to Pitfall 3(a) SER-02 (`diff.py` empty-side rule) | ✓ VERIFIED | Pitfall 3(a) block at `diff.py:467-472` UNCHANGED; soft-paired pairs route through `_emit_leaf_diffs` (diff.py:527-528) — same helper exact-match pairs use; 3 hand-fill survival tests only pass because this fall-through works |
| SC#4 | Real drift still raises | ✓ VERIFIED | `test_real_drift_handfilled_position_recomputed_to_different_non_empty_still_raises` + `test_real_drift_non_handfill_collector_change_still_raises` PASS; soft-pair scalar-alignment check at `diff.py:345-353` requires non-empty positions to match exactly |
| SC#5 | Multi-client topologies pair correctly + ambiguous fallback | ✓ VERIFIED | `test_multi_client_topology_two_handfilled_three_total_pairs_correctly` + `test_ambiguous_soft_pair_two_candidates_falls_back_to_orphans` + `test_partial_ambiguity_unique_then_ambiguous_does_not_greedily_conflate` all PASS — two-phase algorithm prevents conflation |
| SC#6 | `TestDiffHandFillAffordance` class with documented test methods | ✓ VERIFIED | Class at `test_diff.py:460` contains 14 methods (12 originally specified + 2 follow-up regressions); all listed test names present |
| SC#7 | RED-first discipline | ✓ VERIFIED | `git log --oneline -6`: RED commit `3e84bec test(05.2-01)` precedes GREEN commit `98c265e feat(05.2-01)`; follow-up RED `9a62c1a test(05.2-02)` precedes follow-up GREEN `01d7d1e fix(05.2-02)` |
| SC#8 | `pytest tests/unit` GREEN modulo documented pre-existing failures | ✓ VERIFIED | `pytest tests/unit --ignore=test_benchmarks_base.py --ignore=test_parquet_reader.py --ignore=test_vdb_modular_fake_backend.py` → **1998 passed, 4 skipped, 7 failed**. All 7 failures are in the documented pre-existing set (5 in test_datagen_command_generation.py, 2 in test_rules_calculations.py::TestGenerateOutputLocation). NO new failures. |
| SC#9 | Phase 5 success criteria #1-#8 unchanged | ✓ VERIFIED | 29 pre-existing tests in test_diff.py (TestDataclasses, TestFlattenToPaths, TestRoundTripEqualIsEmpty, TestFieldChangeIsDrift, TestQuantityChangeIsDrift, TestSymmetricDriftDetection, TestSer02BlankPreservation, TestUnifiedDiffFormat) all continue to PASS post-GREEN |
| D-60 | Reverse-direction INFO log scoped to fingerprint scalar paths | ✓ VERIFIED | `_FINGERPRINT_SCALAR_PATHS` frozenset declared at `diff.py:140-142`; D-60 branch at `diff.py:447-462` gates on `path in _FINGERPRINT_SCALAR_PATHS`; `test_d60_does_not_swallow_non_fingerprint_leaf_changes` PASSES (locks WR-01 fix) |
| D-61 | Signature strict-match | ✓ VERIFIED | `signature_positions` strict-equality check at `diff.py:336`; `test_signature_strict_match_empty_signature_does_not_soft_pair` + `test_signature_strict_match_different_sysctl_sigs_do_not_soft_pair` both PASS |
| D-62 | Two-pass pairing (exact-match first, then soft-pair) | ✓ VERIFIED | Three-pass control flow in `diff_node_dict_lists` (diff.py:521-549); pass-1 fast-path locked by `test_exact_match_pass_still_preserves_identical_fingerprints_no_soft_pair_invoked` |
| D-63 | Ambiguous fallback (never silently conflate) — TWO-PHASE algorithm | ✓ VERIFIED | Phase A enumeration at `diff.py:328-363` (no consumption); Phase B `disk_claim_count` commit at `diff.py:365-391` (rejects contested pairs); `test_partial_ambiguity_unique_then_ambiguous_does_not_greedily_conflate` PASSES (locks CR-01 fix) |
| HANDFILL-01 | Closed in REQUIREMENTS.md | ✓ VERIFIED | REQUIREMENTS.md line lists `[x] **HANDFILL-01**` (checked); requirement description matches the implemented contract verbatim |

**Score:** 14/14 truths verified.

## Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `mlpstorage_py/system_description/diff.py` | `_soft_pair_orphans` + `_emit_leaf_diffs` + module logger + `_FINGERPRINT_SCALAR_PATHS` + soft-pair integrated into `diff_node_dict_lists` | ✓ VERIFIED | All five symbols present; two-phase algorithm verified (Phase A enumerate, Phase B claim_count commit); D-60 scoped to `_FINGERPRINT_SCALAR_PATHS` |
| `tests/unit/test_diff.py` | `class TestDiffHandFillAffordance` with 12+2 test methods | ✓ VERIFIED | 14 test methods present (12 original + 2 follow-up); 43 total tests in file; 43/43 PASS |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `TestDiffHandFillAffordance` test methods | `diff_node_dict_lists` | Direct call with hand-filled fixture stanzas | ✓ WIRED | Every test method imports + invokes `diff_node_dict_lists` and asserts on `.empty` / `.entries` |
| `diff_node_dict_lists` pass-2 | `_emit_leaf_diffs` | `for mem_fp, disk_fp in sorted(paired_fp_pairs): _emit_leaf_diffs(...)` at diff.py:527-528 | ✓ WIRED | Soft-paired stanzas fall through the same helper exact-match pairs use, so Pitfall 3(a) preserves submitter values |
| `_soft_pair_orphans` Phase B | `disk_claim_count` gating | `if disk_claim_count[disk_fp] != 1: continue` at diff.py:387 | ✓ WIRED | Two-phase commit rejects contested pairs (CR-01 fix) |
| `_emit_leaf_diffs` D-60 branch | `_FINGERPRINT_SCALAR_PATHS` frozenset | `path in _FINGERPRINT_SCALAR_PATHS` at diff.py:448 | ✓ WIRED | D-60 INFO log scoped to fingerprint scalar paths only (WR-01 fix) |
| Diff module logger | stdlib `logging` | `logger = logging.getLogger(__name__)` at diff.py:116 | ✓ WIRED | Logger name resolves to `mlpstorage_py.system_description.diff`; matches caplog filter in reverse-direction tests |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
| -------- | ---- | ------ | ------------------ | ------ |
| `diff_node_dict_lists` | `DiffResult.entries` | Comparison of input `on_disk` and `in_memory` stanza lists via `_compute_fingerprint` indexing, then `_emit_leaf_diffs` walking flattened paths | Yes — entries are dynamically produced from inputs; behavior covered by 43 unit tests | ✓ FLOWING |
| `_soft_pair_orphans` | `paired_fp_pairs` | Two-phase candidate enumeration + claim-count gating against actual fingerprint tuples | Yes — verified by 14 TestDiffHandFillAffordance tests with diverse fixture inputs | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All TestDiffHandFillAffordance tests pass | `pytest tests/unit/test_diff.py::TestDiffHandFillAffordance -v` | 14/14 PASSED | ✓ PASS |
| All test_diff.py tests pass (43 total: 29 pre-existing + 14 new) | `pytest tests/unit/test_diff.py -v` | 43 passed in 0.05s | ✓ PASS |
| Broader unit test sweep, no NEW failures | `pytest tests/unit --ignore=test_benchmarks_base.py --ignore=test_parquet_reader.py --ignore=test_vdb_modular_fake_backend.py` | 1998 passed, 4 skipped, 7 failed (all 7 in documented pre-existing set) | ✓ PASS |
| Source-grounding: `_soft_pair_orphans` defined | `grep -c "def _soft_pair_orphans(" diff.py` | 1 | ✓ PASS |
| Source-grounding: D-63 two-phase algorithm | `grep -c "disk_claim_count" diff.py` | 3 (dict init + tally + check) | ✓ PASS |
| Source-grounding: D-60 scoped to fingerprint scalars (WR-01 fix) | `grep -c "_FINGERPRINT_SCALAR_PATHS" diff.py` | 2 (frozenset definition + path-membership check) | ✓ PASS |
| Source-grounding: module logger | `grep -c "logger = logging.getLogger" diff.py` | 1 | ✓ PASS |
| Source-grounding: test class | `grep -c "class TestDiffHandFillAffordance" test_diff.py` | 1 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| HANDFILL-01 | 05.2-01-PLAN.md | Diff-layer soft-pair pre-pass; hand-filled scalar fingerprint positions survive re-run with empty-collector; reverse-direction INFO log; real drift still raises | ✓ SATISFIED | REQUIREMENTS.md checklist marks `[x] HANDFILL-01`; full requirement text matches implemented contract (`_FINGERPRINT_KEYS` 7 scalar positions, D-61 strict-signature, D-63 ambiguous fallback, D-60 INFO log, real drift still raises). All 14 TestDiffHandFillAffordance tests + 29 pre-existing TestDiff* tests PASS. |

No orphaned requirements. HANDFILL-01 is the only Phase 5.2 requirement and is fully satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none in the files modified by this phase) | — | — | — | — |

No debt markers (TBD/FIXME/XXX) or unresolved TODOs in `diff.py` or `test_diff.py`. Code is well-commented with explicit references to D-60..D-66 decisions, CR-01/WR-01 fixes, and the SER-02 Pitfall 3(a) contract.

### Re-Verification of Code-Review Resolution

The REVIEW.md frontmatter lists CR-01, WR-01, WR-02, WR-04 as `resolved` and WR-03, IN-01..IN-03 as `deferred`. Re-verification confirms:

- **CR-01 (greedy conflation):** Two-phase algorithm at `diff.py:328-391` (Phase A enumerate + Phase B `disk_claim_count` commit) closes the greedy bug. `test_partial_ambiguity_unique_then_ambiguous_does_not_greedily_conflate` (test_diff.py:668) provides the regression lock and PASSES. **CLOSED.**
- **WR-01 (D-60 over-scope):** D-60 branch gated by `path in _FINGERPRINT_SCALAR_PATHS` at `diff.py:447-448`. `test_d60_does_not_swallow_non_fingerprint_leaf_changes` (test_diff.py:720) provides the regression lock and PASSES. **CLOSED.**
- **WR-02 (test coverage gap):** Both regression tests added (CR-01 + WR-01 scenarios). **CLOSED.**
- **WR-04 (f-string in logger):** `logger.info("... %s=%r ...", path, mem_v)` at `diff.py:456-461` uses deferred %-style formatting. **CLOSED.**
- **WR-03 / IN-01..IN-03:** Quality-only items deferred per REVIEW.md; do not block goal achievement. Not in scope of this verification.

### Human Verification Required

None. All goal-bearing behavior is locked by automated unit tests against the pure-function diff core. The phase touches no MPI, no filesystem I/O, no UI surface, and no operator workflow — `diff.py` is a pure transformation layer. The lifecycle wiring at `auto_generator.py:874` was unchanged by this phase per the plan; Phase 5's existing UAT coverage of the SystemDriftError raise path remains the user-visible contract.

The D-60 INFO log is operator-facing but its content is locked by `test_reverse_direction_info_log_includes_field_path_and_resolved_value` asserting the field path, resolved value, and `LIFE-04` hint substrings appear in the log message.

### Gaps Summary

No gaps. The phase goal is observably true:

1. Hand-filled scalar fingerprint values survive a re-run when the collector returns `""` — locked by 3 hand-fill survival tests.
2. Real hardware drift still raises — locked by 2 real-drift tests.
3. Ambiguous candidates fall back to orphan emission without conflation — locked by 2 ambiguity tests (including the CR-01 regression test).
4. D-60 reverse-direction log fires (and is scoped to fingerprint scalars only) when the collector learns a value — locked by 3 D-60 tests (including the WR-01 regression test).
5. Phase 5 behavior is unchanged — locked by all 29 pre-existing test_diff.py tests continuing to PASS.

### Minor observation (NOT a gap)

The REQUIREMENTS.md table-row status for HANDFILL-01 still reads "Pending — diff-layer soft-pair pre-pass; ..." while the checklist marker is `[x]`. The checklist marker is load-bearing per project convention; the table-row description appears stale. This is documentation-only and does not affect goal achievement.

---

_Verified: 2026-06-25_
_Verifier: Claude (gsd-verifier)_
