---
phase: 02-first-run-write-of-partial-systemname-yaml
verified: 2026-06-20T13:15:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/7
  gaps_closed:
    - "A real Benchmark.run() invocation on the production single-node fallback path produces a systemname.yaml without crashing"
    - "A first-run mlpstorage <bench> <model> datagen invocation completes successfully (no regression from Phase 1 — Phase 2 must not break the datagen path)"
  gaps_remaining: []
  regressions: []
  fix_commits:
    - "13f44e3 — test(02-06): RED regression test exposing CR-01 AttributeError on uninitialized _cluster_info_start"
    - "42d1506 — fix(02-06): initialize _cluster_info_start=None in Benchmark.__init__ to close CR-01"
gaps: []
deferred:
  - truth: "to_dict() round-trips num_sockets through JSON metadata"
    addressed_in: "Phase 5 (LIFE-02 logical-diff round-trip) / WR-03 follow-up"
    evidence: "Affects ONLY the per-run JSON metadata serialization path (rules/models.py:271-277). The systemname.yaml emit path reads HostInfo fields directly and does NOT go through to_dict(); SER-01 grouping is satisfied for first-run. Recorded by code review as WR-03 (warning, not blocker). Will affect Phase 5 LIFE-02 logical-diff round-trip but is not required for the Phase 2 phase goal."
  - truth: "Latent quality warnings WR-01 (path traversal in --systemname), WR-02 (dry-run gate on hook), WR-04 (over-broad except Exception in hook), WR-05 (D-8 fallback has no try/except around collect_local_system_info), IN-01/IN-03/IN-04 (dead import, hot-path import, silent system_info data drop in _from_metadata)"
    addressed_in: "Follow-up work — out of scope for Phase 2 gap closure"
    evidence: "Per 02-VERIFICATION.md (initial) conclusion: 'should be addressed in a follow-up — particularly WR-02 (dry-run gate) and WR-03 (to_dict round-trip), both of which have downstream impact on Phase 5's logical-diff implementation.' Gap-closure plan 02-06 explicitly scoped these out (see 42d1506 commit message: 'Out of scope: WR-01..WR-05, IN-01/IN-03/IN-04 are deliberately untouched per the gap_closure_context in 02-06-PLAN.md')."
human_verification:
  - test: "Run mlpstorage closed training unet3d run file --results-dir /tmp/r1 --systemname sys-v1 --num-accelerators 2 --accelerator-type h100 --client-host-memory-in-gb 64 --data-dir /databases/mlps-v3.0/data/ on a real single-node box with no MPI installed (no --hosts override). Confirm the file appears at /tmp/r1/closed/<orgname>/systems/sys-v1.yaml BEFORE DLIO launches."
    expected: "File exists, contains filled CPU/memory/OS fields, has blank SER-02 stubs for friendly_description/chassis.model_name/networking[]/drives[]. Benchmark proceeds to run without aborting."
    why_human: "End-to-end integration with real MPI / real cluster collector / real psutil cannot be programmatically verified — the dev environment lacks psutil and the integration tests all mock the cluster collector. Automated CR-01 regression test confirms the AttributeError no longer fires (the underlying invariant); end-to-end run on a real box validates real /proc/cpuinfo populates the filled fields."
  - test: "Run mlpstorage closed training unet3d datagen file --results-dir /tmp/r1 --systemname sys-v1 ... on a real system and confirm the command completes without AttributeError."
    expected: "Per the phase goal (LIFE-01), datagen MUST not write systemname.yaml AND must NOT crash. The expected behavior is that datagen succeeds with no file at the systemname.yaml path."
    why_human: "Automated regression test test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_datagen confirms the no-AttributeError invariant; a real-system smoke test confirms the end-to-end datagen path remains intact (the original Phase 1 invariant)."
  - test: "Run mlpstorage vectordb run ... --systemname sys-v1 ... (no --hosts) on a real system."
    expected: "Per the phase goal, the file should appear before DLIO launches via the D-8 single-host fallback."
    why_human: "Automated test test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_run confirms the AttributeError-no-longer-fires invariant and that the D-8 fallback writes the file via collect_local_system_info; a real run validates psutil-based local collection populates real fields."
  - test: "Inspect the emitted systemname.yaml against schema_validator.validate_file() and confirm errors appear ONLY on the intentional SER-02 blanks (friendly_description, chassis.model_name, networking[].*, drives[].*, solution, deployment) and NEVER on cpu_model/cpu_qty/cpu_cores/memory_capacity/operating_system.name/operating_system.version."
    expected: "Validator returns a list[str] of error strings whose dotted paths are all in the blank-list and none on the filled-list."
    why_human: "test_validator_errors_only_on_blanks covers this with mocked data; needs human confirmation that the real cluster collector populates the filled fields end-to-end (verifies SER-03 success criterion #4 against real /proc/cpuinfo content)."
---

# Phase 02: First-Run Write of Partial systemname.yaml — Verification Report

**Phase Goal:** A submitter who has run `mlpstorage init <orgname> <results-dir>` (per Phase 1) and then runs `mlpstorage <mode> <benchmark> <model> run --results-dir <results-dir> --systemname <sys> ...` for the first time finds a quantity-grouped `<results-dir>/<mode>/<orgname>/systems/<sys>.yaml`, populated with the CPU, memory, and OS fields the existing MPI collector already gathers — with any blanks visibly waiting for them.

**Verified:** 2026-06-20T13:15:00Z
**Status:** passed
**Re-verification:** Yes — after Phase 2 gap closure (02-06)

## Re-Verification Summary

The initial verification on 2026-06-20 returned `status: gaps_found` with `score: 5/7` based on CR-01 — an `AttributeError` at `mlpstorage_py/benchmarks/base.py:991` triggered when `_collect_cluster_start()` took its early-return branch (datagen/configview commands or any benchmark whose `--hosts` defaults to None, e.g. VectorDB). Two truths were affected: the datagen-no-crash invariant (FAILED) and the production single-node-fallback for run (PARTIAL); both shared CR-01 as the root cause.

Gap-closure plan 02-06 landed two commits:

- **`13f44e3` test(02-06):** Two RED regression tests in `tests/integration/test_systemname_yaml_end_to_end.py` (`test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_datagen` and `..._run`) that construct a VectorDB benchmark WITHOUT mocking `_collect_cluster_start`, exposing the early-return path and the bug.
- **`42d1506` fix(02-06):** A single-line init in `Benchmark.__init__` at `base.py:155`: `self._cluster_info_start = None`, adjacent to the existing `self._cluster_collector = cluster_collector` assignment. CR-01 Option A from the review; matches the `missing:` block in the initial verification.

Both regression tests now PASS; the existing 12 integration tests continue to PASS; the 2 per-benchmark hook unit tests (KVCache + VectorDB) continue to PASS. The CR-01 BLOCKER is RESOLVED. Truths 1 and 6 flip PARTIAL → VERIFIED. Truth 2 flips FAILED → VERIFIED. LIFE-01 flips BLOCKED → SATISFIED. Score is now 7/7.

The four human verification items remain in scope and unchanged: the fix closes the automated invariant (no AttributeError), but real-system smoke tests on real MPI / real psutil / real /proc/cpuinfo content still belong to the operator.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A Benchmark.run() invocation with args.command='run' produces a systemname.yaml at the canonical path BEFORE _start_timeseries_collection() / DLIO                                             | ✓ VERIFIED | Hook at base.py:999; ordered between `_collect_cluster_start` (line 991) and `_start_timeseries_collection` (line 1015). `test_full_run_writes_systemname_yaml` passes. NEW: `test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_run` (commit 13f44e3) exercises the unmocked early-return path with `command='run'` and no `--hosts`, confirming D-8 fallback writes the file via `collect_local_system_info` after the init-side fix (commit 42d1506).      |
| 2   | A Benchmark.run() invocation with args.command='datagen' does NOT produce a systemname.yaml — does not crash either                                                                            | ✓ VERIFIED | `test_datagen_does_not_write` passes (existing mocked path). NEW: `test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_datagen` (commit 13f44e3) exercises the unmocked datagen early-return path: `bm.run()` returns 0 with no AttributeError, and the canonical D-11 path is empty. D-12 command gate (auto_generator.py:443) fires cleanly. Phase 1 datagen invariant preserved.                                                                              |
| 3   | A second Benchmark.run() invocation against the same results-dir + systemname is a no-op (D-9 FileExistsError → return None)                                                                   | ✓ VERIFIED | auto_generator.py:480-485 catches FileExistsError and returns None with logger.debug. `test_second_run_no_overwrite` passes (14-test integration suite green).                                                                                                                                                                                                                                                                                                                       |
| 4   | Per-mode separation: invoking under mode='closed' vs mode='whatif' produces two independent files at different paths (D-11)                                                                    | ✓ VERIFIED | Path construction at auto_generator.py:461-467 uses `args.mode` as the second path segment. `test_per_mode_separation` (parametrized over closed/open/whatif) passes.                                                                                                                                                                                                                                                                                                                  |
| 5   | schema_validator.validate_file() against the emitted YAML reports errors ONLY on intentional blanks (friendly_description, chassis.model_name, networking[], drives[]) — never on filled fields | ✓ VERIFIED | `test_validator_errors_only_on_blanks` parses `validate_file`'s `list[str]` return and asserts the dotted paths are all in the blank-list. Filled-field paths (chassis.cpu_model, chassis.cpu_qty, chassis.cpu_cores, chassis.memory_capacity, operating_system.name, operating_system.version) are never in the error list. SER-03 #4 locked.                                                                                                                                          |
| 6   | Non-DLIO benchmarks (KVCacheBenchmark, VectorDBBenchmark) ALSO write the systemname.yaml because the hook lives in the shared Benchmark.run()                                                  | ✓ VERIFIED | `TestKVCacheSystemnameYamlHook::test_kvcache_run_writes_systemname_yaml` and `TestVectorDBSystemnameYamlHook::test_vectordb_run_writes_systemname_yaml` pass. NEW: `test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_run` constructs a `VectorDBBenchmark` (the benchmark whose `--hosts` defaults to None per `cli/vectordb_args.py:107`) and verifies the file IS written via the D-8 local fallback — closing the VectorDB-no-hosts gap from CR-01.       |
| 7   | Filesystem failures in write_systemname_yaml (not FileExistsError) abort Benchmark.run() BEFORE DLIO/MPI launches                                                                              | ✓ VERIFIED | base.py:1005-1015 catches `Exception` and re-raises (does NOT swallow). `test_filesystem_failure_propagates` uses `patch('os.open', side_effect=PermissionError)` and asserts `bm._run` is not called. The over-broad `except Exception` (WR-04) remains a quality concern (deferred) but the contract — non-FileExistsError aborts — is honored.                                                                                                                                       |

**Score:** 7/7 truths VERIFIED

### Deferred Items

| #   | Item                                                                                                                                                                                                          | Addressed In                                            | Evidence                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | HostInfo.to_dict() round-trips num_sockets through JSON metadata (WR-03)                                                                                                                                       | Phase 5 (LIFE-02 logical-diff round-trip) / WR-03 FU    | Affects per-run JSON metadata serialization (rules/models.py:271-277). The systemname.yaml path reads HostInfo fields directly — does NOT go through to_dict(). Will impact Phase 5 LIFE-02 logical diff but not Phase 2.                              |
| 2   | Latent quality warnings WR-01 (path traversal), WR-02 (dry-run gate), WR-04 (over-broad except), WR-05 (D-8 try/except), IN-01/IN-03/IN-04 (hot-path import, dead import, _from_metadata silent data drop) | Follow-up work                                          | Initial verification flagged these as "should be addressed in a follow-up — particularly WR-02 and WR-03." Gap closure 02-06 deliberately scoped them out (commit 42d1506 message: "Out of scope: WR-01..WR-05, IN-01/IN-03/IN-04 deliberately untouched"). |

### Required Artifacts

| Artifact                                                  | Expected                                                                                                | Status               | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mlpstorage_py/rules/models.py`                           | HostCPUInfo with num_sockets field; HostInfo.from_collected_data wires num_sockets through              | ✓ EXISTS + SUBSTANTIVE | num_sockets field at line 156 (default 0); from_dict reads it at line 166; from_collected_data wires it at line 221. SER-01 grouping is sourced from this. to_dict() is missing the field (WR-03, deferred).                                                                                                                                                                                                                                                |
| `mlpstorage_py/system_description/auto_generator.py`      | node_dict_from_host, group_by_fingerprint, _build_outer_dict, _splice_stub_lists, write_systemname_yaml | ✓ EXISTS + SUBSTANTIVE | All five symbols present. Atomic O_CREAT\|O_EXCL\|O_WRONLY pattern correct (lines 474-485). D-11 path construction (lines 461-467) correct. D-7 sort (line 454) correct. **D-8 fallback at lines 361-378 unchanged** — still correctly handles `cluster_info=None` via `collect_local_system_info`, which the writer now reaches after the base.py:155 init-side fix. D-12 command gate at line 443 correct.                                                          |
| `mlpstorage_py/benchmarks/base.py`                        | Init of `_cluster_info_start = None` in __init__; call to write_systemname_yaml between cluster-start and timeseries-start | ✓ EXISTS + WIRED       | **NEW (commit 42d1506):** `self._cluster_info_start = None` at line 155, adjacent to `self._cluster_collector = cluster_collector` at line 147. Comment block at lines 148-154 documents the Phase 2 LIFE-01 rationale and links the D-8 fallback. Import at line 67; call at line 999. Position correct (between cluster-start at 991 and timeseries-start at 1015). Defensive `hasattr` at line 670 in `_collect_cluster_end` continues to work consistently. |
| `tests/integration/test_systemname_yaml_end_to_end.py`    | 14 tests (12 existing + 2 new regression covering unmocked early-return path)                            | ✓ EXISTS + WIRED       | **NEW (commit 13f44e3, finalized in 42d1506):** Two regression tests at lines 490 and 528 that omit the `_collect_cluster_start` mock, construct a real `VectorDBBenchmark`, and assert (a) `hasattr(bm, '_cluster_info_start')` is True post-init, (b) `bm._cluster_info_start is None`, (c) `bm.run()` returns 0 without AttributeError, (d) datagen-subcase: no file written; (e) run-subcase: file IS written via D-8 fallback. 14/14 PASS.                |
| `tests/unit/test_benchmarks_kvcache.py`                   | Extended assertion that KVCacheBenchmark.run() fires write_systemname_yaml                              | ✓ EXISTS + WIRED       | `TestKVCacheSystemnameYamlHook::test_kvcache_run_writes_systemname_yaml` PASS.                                                                                                                                                                                                                                                                                                                                                                                       |
| `tests/unit/test_benchmarks_vectordb.py`                  | Extended assertion that VectorDBBenchmark.run() fires write_systemname_yaml                             | ✓ EXISTS + WIRED       | `TestVectorDBSystemnameYamlHook::test_vectordb_run_writes_systemname_yaml` PASS. NEW regression test in integration suite additionally exercises VectorDB without mock.                                                                                                                                                                                                                                                                                            |

### Key Link Verification

| From                                                                  | To                                                                              | Via                                                                | Status         | Details                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| benchmarks/base.py:Benchmark.run (line 999)                            | system_description/auto_generator.py:write_systemname_yaml                       | `write_systemname_yaml(self.args, self._cluster_info_start, ...)`   | ✓ WIRED        | Import at line 67. Call site syntactically correct. **Attribute access pattern now safe** — `self._cluster_info_start` is initialized to None in `__init__` (line 155), so the read always succeeds. D-8 fallback in writer takes over on None. |
| benchmarks/base.py:Benchmark.__init__ (line 155)                       | benchmarks/base.py:Benchmark.run hook (line 999)                                 | `self._cluster_info_start = None` (init-side default)               | ✓ WIRED        | NEW (CR-01 fix): the init-side default closes the latent AttributeError window for both (a) the early-return at base.py:644-646 and (b) the symmetric defensive `hasattr` at line 670 in `_collect_cluster_end`.                                |
| auto_generator.py:write_systemname_yaml                                | auto_generator.py:_resolve_host_info_list (D-8 fallback)                        | `hosts = _resolve_host_info_list(cluster_info)` at line 447         | ✓ WIRED        | Correctly invoked. `_resolve_host_info_list(None)` at line 374-378 falls back to `collect_local_system_info()`. Behavior reached on real VectorDB-no-hosts path by the new regression test.                                                       |
| auto_generator.py:node_dict_from_host                                  | rules/models.py:HostCPUInfo.num_sockets                                          | `host.cpu.num_sockets` at line 206                                  | ✓ WIRED + FLOWING | Read at line 206 → emitted as chassis.cpu_qty per D-16. Field exists on HostCPUInfo (rules/models.py:156). Populated by from_collected_data (line 221) from summarize_cpuinfo().                                                                   |
| auto_generator.py:node_dict_from_host                                  | rules/models.py:HostInfo.system.os_release                                       | `host.system.os_release.get("NAME"), .get("VERSION_ID")` at lines 221-222 | ✓ WIRED + FLOWING | Pitfall 4 / COLL-02 wiring correct: NAME → name, VERSION_ID → version, defensively defaulted to "".                                                                                                                                              |
| auto_generator.py:write_systemname_yaml                                | filesystem at `<results_dir>/<mode>/<orgname>/systems/<systemname>.yaml`         | os.open(O_CREAT\|O_EXCL\|O_WRONLY) at line 475                       | ✓ WIRED        | Atomic create pattern matches sentinel.py. T-2-01 race + T-2-08 symlink covered in one syscall. Parent dirs created via mkdir(parents=True, exist_ok=True) at line 468.                                                                          |

### Data-Flow Trace (Level 4)

| Artifact                                              | Data Variable                  | Source                                                                                                                                                                                              | Produces Real Data | Status                                                                                                          |
| ----------------------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------- |
| auto_generator.py:write_systemname_yaml             | cluster_info.host_info_list    | base.py:Benchmark.run passes `self._cluster_info_start` which is populated by `_collect_cluster_information` (real MPI / SSH collection) OR the init-side default of None which routes through the D-8 fallback to `collect_local_system_info` | YES                | ✓ FLOWING — happy path AND single-node fallback paths both terminate in real-data collection. Datagen path correctly returns None at the D-12 gate without crashing. |
| auto_generator.py:node_dict_from_host               | host.cpu, host.memory, host.system | rules/models.py:HostInfo.from_collected_data which reads MPI collector output (cpuinfo, meminfo, os_release)                                                                                       | YES                | ✓ FLOWING — real /proc/cpuinfo summary → HostCPUInfo with num_sockets, num_cores, model, architecture                |
| auto_generator.py:group_by_fingerprint              | items, fingerprint_keys        | List of node_dict_from_host outputs; fingerprint_keys is the module-level _FINGERPRINT_KEYS constant                                                                                                | YES                | ✓ FLOWING — quantity-grouping works on real data per integration tests                                                |

### Behavioral Spot-Checks

| Behavior                                                                     | Command                                                                                                                  | Result                                                                                                          | Status      |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- | ----------- |
| Integration test suite (12 existing + 2 new regression) passes               | `python3 -m pytest tests/integration/test_systemname_yaml_end_to_end.py -q`                                              | 14 passed in 0.33s                                                                                              | ✓ PASS      |
| Per-benchmark hook unit tests (KVCache + VectorDB) pass                      | `python3 -m pytest tests/unit/test_benchmarks_kvcache.py::TestKVCacheSystemnameYamlHook tests/unit/test_benchmarks_vectordb.py::TestVectorDBSystemnameYamlHook -q` | 2 passed in 0.16s                                                                                               | ✓ PASS      |
| _cluster_info_start initialization in __init__                               | `grep -n "self\._cluster_info_start" base.py`                                                                            | line 155 (init), line 654 (SSH-branch assign), 658 (MPI-branch assign), 661 (truthy check), 670 (hasattr defensive), 689 (consumer), 695 (alias), 999 (Phase 2 write hook) | ✓ PASS      |
| Init-side fix is adjacent to existing _cluster_collector init                | `grep -B 1 -A 1 "_cluster_collector = cluster_collector" base.py:147-156`                                                | line 147 _cluster_collector init, lines 148-154 explanatory comment, line 155 `self._cluster_info_start = None` | ✓ PASS      |
| New regression test exists (datagen-subcase) and asserts no-AttributeError + no-file-written | `grep -n "test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_datagen" tests/integration/test_systemname_yaml_end_to_end.py` | line 490                                                                                                        | ✓ PASS      |
| New regression test exists (run-subcase) and asserts no-AttributeError + file-IS-written via D-8 | `grep -n "test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_run" tests/integration/test_systemname_yaml_end_to_end.py`     | line 528                                                                                                        | ✓ PASS      |
| Both new tests omit the `_collect_cluster_start` mock                        | `grep -n "_make_benchmark_no_cluster_mock" tests/integration/test_systemname_yaml_end_to_end.py`                          | helper defined at line 460-ish; called at 498, 539 (datagen + run subcases)                                     | ✓ PASS      |
| D-8 fallback path in writer is unchanged                                     | `git diff 1c5ae04..HEAD -- mlpstorage_py/system_description/auto_generator.py`                                          | (empty — writer not modified in gap closure)                                                                    | ✓ PASS      |
| Fix commits exist                                                            | `git log --oneline 13f44e3 42d1506`                                                                                       | both present in branch history                                                                                  | ✓ PASS      |

### Probe Execution

| Probe                                | Command                                                                                | Result               | Status |
| ------------------------------------ | -------------------------------------------------------------------------------------- | -------------------- | ------ |
| Phase 2 integration test suite       | `python3 -m pytest tests/integration/test_systemname_yaml_end_to_end.py -q`            | 14 passed in 0.33s   | ✓ PASS |
| Phase 2 per-benchmark hook unit tests | `python3 -m pytest tests/unit/test_benchmarks_kvcache.py::TestKVCacheSystemnameYamlHook tests/unit/test_benchmarks_vectordb.py::TestVectorDBSystemnameYamlHook -q` | 2 passed in 0.16s    | ✓ PASS |
| (No conventional `scripts/*/tests/probe-*.sh` declared for this phase — N/A) | —                                                                                      | —                    | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan(s)            | Description                                                                                                                                                       | Status        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| COLL-01     | 02-01, 02-02, 02-05       | Collector exposes clients[].chassis.cpu_model, cpu_qty, cpu_cores, memory_capacity per host                                                                       | ✓ SATISFIED   | HostCPUInfo.num_sockets wired through from_collected_data → node_dict_from_host emits chassis.cpu_qty from host.cpu.num_sockets (D-16). cpu_model/cpu_cores wired from host.cpu.model / host.cpu.num_cores. memory_capacity wired from host.memory.total via D-6 GiB rounding. Universal-failure rule honored: missing data → "".                                                                                                                                                                                                                                                                                |
| COLL-02     | 02-02, 02-05              | Collector exposes clients[].operating_system.name and version per host                                                                                            | ✓ SATISFIED   | node_dict_from_host at auto_generator.py:221-222 reads os_release.NAME → name, os_release.VERSION_ID → version (Pitfall 4 lock). Defensive `.get(k, "") or ""` collapses missing/None to "".                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SER-01      | 02-02, 02-05              | Per-host collected data is quantity-grouped before serialization                                                                                                  | ✓ SATISFIED   | group_by_fingerprint at auto_generator.py:112 + _FINGERPRINT_KEYS at line 78. test_homogeneous_fleet_quantity_equals_fleet_size, test_heterogeneous_fleet_produces_multiple_stanzas pass. Heterogeneous fleets produce multiple stanzas; quantity field sums to fleet size.                                                                                                                                                                                                                                                                                                                                              |
| SER-02      | 02-03, 02-05              | Schema fields that cannot be auto-derived (friendly_description, chassis.model_name, networking, drives) are emitted as blank/missing                             | ✓ SATISFIED   | _NETWORKING_STUB and _DRIVE_STUB at auto_generator.py:259-274 emit empty-string sentinel values. _splice_stub_lists injects one stub each into every client. _build_outer_dict at line 305 OMITS solution/deployment/product_nodes per D-14. test_validator_errors_only_on_blanks confirms the writer-emitted blanks are exactly what the validator flags.                                                                                                                                                                                                                                                              |
| SER-03      | 02-04, 02-05              | The serialized YAML's filled fields validate against schema_validator using the existing validator                                                                | ✓ SATISFIED   | test_validator_errors_only_on_blanks asserts that validate_file's error strings ONLY reference intentional-blank dotted paths and NEVER reference the filled-field dotted paths (chassis.cpu_model, chassis.cpu_qty, chassis.cpu_cores, chassis.memory_capacity, operating_system.name, operating_system.version).                                                                                                                                                                                                                                                                                                          |
| LIFE-01     | 02-04, 02-05, 02-06       | At the start of `run`, the benchmark builds the systemname.yaml from MPI-collected data and writes if missing; non-run commands do NOT write                       | ✓ SATISFIED (flipped from BLOCKED) | Hook in shared Benchmark.run at base.py:999 with try/except wrapper. D-12 command-gate in writer at auto_generator.py:443. **CR-01 RESOLVED** via init-side default `self._cluster_info_start = None` at base.py:155 (commit 42d1506). Two new regression tests at integration suite lines 490 and 528 (commit 13f44e3) prove (a) datagen no-write-no-crash, (b) run-with-no-hosts writes-via-D-8-fallback. Phase 1 datagen invariant preserved; production single-node fallback path produces real systemname.yaml without crashing. |

**All 6 requirement IDs are accounted for in PLAN frontmatter. No orphaned requirements detected.**

### Anti-Patterns Found

| File                                                                       | Line     | Pattern                                                                                       | Severity              | Impact                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| mlpstorage_py/benchmarks/base.py                                           | 999 (was 991) | `self._cluster_info_start` direct attribute read with no init in __init__                  | ✓ RESOLVED            | **CR-01 BLOCKER closed by commits 13f44e3 (RED regression test) + 42d1506 (init-side fix at line 155).** Attribute is now initialized to None in `__init__`, so the read at the hook site always succeeds. D-8 fallback in writer handles None.   |
| mlpstorage_py/benchmarks/base.py                                           | 1005     | Bare `except Exception` catch around write call                                                | ⚠ WARNING (deferred)  | WR-04 — relabels programmer errors (AttributeError, TypeError, KeyError) as "Failed to write systemname.yaml: ..." obscuring the actual fault. Less severe now that CR-01 root cause is fixed; remains a quality concern for follow-up.        |
| mlpstorage_py/rules/models.py                                              | 271-277  | to_dict() omits num_sockets in the serialized cpu dict                                        | ⚠ WARNING (deferred)  | WR-03 — re-loading HostInfo from on-disk JSON metadata loses num_sockets. Affects Phase 5 LIFE-02 logical-diff but not Phase 2.                                                                                                                          |
| mlpstorage_py/system_description/auto_generator.py                         | 417-421  | Docstring claims Phase 1 enforces "well-formed systemname"; Phase 1 only enforces non-empty   | ⚠ WARNING (deferred)  | WR-01 — path traversal via `--systemname '../../../tmp/pwned'` is theoretically reachable. Limited blast radius (must be writable by mlpstorage user; O_EXCL blocks overwrites). Misleading contract.                                                  |
| mlpstorage_py/benchmarks/base.py                                           | 993-1015 | No `getattr(self.args, 'dry_run', False)` gate on the write hook                              | ⚠ WARNING (deferred)  | WR-02 — dry-run writes a real systemname.yaml, then a subsequent real run's D-9 no-op-if-exists refuses to overwrite — poisons subsequent run with dry-run-snapshot data. Inconsistent with WR-09 (Phase 1) and timeseries gating.                  |
| mlpstorage_py/system_description/auto_generator.py                         | 361-378  | _resolve_host_info_list D-8 fallback has no try/except around collect_local_system_info       | ⚠ WARNING (deferred)  | WR-05 — collector exception in fallback path aborts the benchmark instead of degrading to blank HostInfo per D-2 universal-collection-failure rule. Test coverage is happy-path only.                                                                  |
| mlpstorage_py/benchmarks/base.py                                           | 42       | `import types` is present but unused anywhere in the file                                     | ℹ INFO (deferred)     | IN-03 — dead import.                                                                                                                                                                                                                                |
| mlpstorage_py/benchmarks/base.py                                           | 351      | `import copy` inside _apply_dotted_overrides body (hot path)                                  | ℹ INFO (deferred)     | IN-01 — should be hoisted to module-level.                                                                                                                                                                                                            |
| mlpstorage_py/rules/models.py                                              | 853-855  | `if 'system_info' in metadata: pass  # TODO: Reconstruct system_info`                          | ℹ INFO (deferred)     | IN-04 — silent data drop in _from_metadata. Pre-existing, but Phase 2 increases value of system_info by adding num_sockets.                                                                                                                              |

**No debt markers (TBD/FIXME/XXX) introduced by gap-closure commits.** The pre-existing IN-04 TODO in `rules/models.py:853-855` is documented and tracked as a deferred follow-up; it is unchanged by Phase 2 work.

### Human Verification Required

Items requiring real-system testing — see `human_verification:` block in frontmatter. The 4 items remain in scope because the automated CR-01 regression confirms the AttributeError-no-longer-fires invariant but cannot exercise real MPI / real psutil / real /proc/cpuinfo / real schema_validator end-to-end:

1. **Training run on real single-node box** — primary user story path. Validates real /proc/cpuinfo populates the filled CPU/memory/OS fields end-to-end.
2. **Training datagen on real system** — confirms the regression-from-Phase-1 is closed: datagen completes without AttributeError and writes no systemname.yaml.
3. **VectorDB run with no --hosts on real system** — confirms the D-8 single-host fallback writes the file via real psutil-based local collection.
4. **schema_validator.validate_file() on real emitted YAML** — confirms SER-03 end-to-end against real /proc/cpuinfo content.

### Re-Verification Conclusion

Phase 02 delivers the pure-transformation core (COLL-01, COLL-02, SER-01, SER-02, SER-03) cleanly, and the lifecycle wiring (LIFE-01) is now SATISFIED following gap-closure plan 02-06.

The CR-01 BLOCKER — `AttributeError` at `Benchmark.run`'s write-hook argument-evaluation step when `_collect_cluster_start()` takes its early-return branch — is RESOLVED by commit `42d1506`'s single-line init-side default `self._cluster_info_start = None` at `mlpstorage_py/benchmarks/base.py:155`. This is CR-01 Option A from the review, the recommendation from the initial verification's `missing:` block, and it closes both production paths the bug affected:

1. **Datagen / configview commands** — `_should_collect_cluster_info()` returns False, the early-return at base.py:644-646 fires, the init-side default keeps `_cluster_info_start` as None, the writer's D-12 command gate (`command != 'run'`) returns None cleanly, no file is written, no AttributeError. Phase 1 datagen invariant preserved.

2. **VectorDB run with no --hosts** (default=None per `cli/vectordb_args.py:107`) — same early-return path, but with `command='run'` the D-12 gate passes through and the D-8 fallback at `auto_generator.py:374-378` calls `collect_local_system_info()` to populate a single HostInfo. The file is written at the canonical D-11 path. Production VectorDB single-node fallback now works.

Commit `13f44e3` adds two regression tests (`test_run_does_not_raise_when_cluster_info_start_attribute_is_uninitialized_datagen` and `..._run` in `tests/integration/test_systemname_yaml_end_to_end.py:490, 528`) that construct a real `VectorDBBenchmark` WITHOUT mocking `_collect_cluster_start`, exposing the early-return path. Both tests fail on HEAD pre-fix with the exact AttributeError; both pass post-fix. The bug-reproduction lock is now part of the regression suite.

The remaining quality warnings (WR-01 through WR-05; IN-01/IN-03/IN-04) and the to_dict() round-trip gap (WR-03) are deliberately deferred — Phase 5's LIFE-02 logical-diff work will surface and address WR-03 directly; the others should be picked up as a follow-up. None block the Phase 2 goal.

**Status: passed. Score: 7/7 must-haves verified. Human verification of the 4 real-system smoke tests remains the operator's responsibility before marking the phase user-acceptance complete.**

---

_Re-verified: 2026-06-20T13:15:00Z_
_Initial verification: 2026-06-20 (gaps_found, 5/7)_
_Verifier: Claude (gsd-verifier)_
