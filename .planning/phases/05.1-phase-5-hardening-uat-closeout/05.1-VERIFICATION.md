---
phase: 05.1-phase-5-hardening-uat-closeout
verified: 2026-06-25T08:00:00Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 6/6
  gaps_closed:
    - "SC#7 (HARDEN-03 init↔env-validator gap closure): args.orgname now consulted before env fallback at code_image.py:563 + symmetric :602 SYSTEMNAME"
    - "SC#8 (HARDEN-04 OpenMPI 4.x tag-output regex closure): tag-strip helper at cluster_collector.py:3313 consumes [rank,jobid]<channel>: prefix; 4/4 real-mpirun integration tests pass GREEN"
  gaps_remaining: []
  regressions: []
requirements:
  - id: HARDEN-01
    status: SATISFIED
    evidence: "RED d13b418 + GREEN e5b7b8b + docs 723442e; dlio.py:533 getattr lazy-collect verified; HARDEN-01 traceability comment at test_capacity_gate.py:350; 32/32 test_capacity_gate.py PASS post-HARDEN-03/04 (cross-plan invariant preserved)."
  - id: HARDEN-02
    status: SATISFIED
    evidence: "RED f55fd5d + GREEN 086b2a9 + docs 7a29797; stdout-marker transport verified (BEGIN×7, END×6, --tag-output×4); misleading 'mpi4py not installed' substring purged (0 occurrences); 4/4 TestSharedFsProbeNonRank0Silence PASS post-HARDEN-03/04 (cross-plan invariant preserved)."
  - id: HARDEN-03
    status: SATISFIED
    evidence: "RED 795a088 + GREEN 3dbc2d3 + docs 993bd11 + SUMMARY a0636a1; code_image.py:563 reads `getattr(args, \"orgname\", None) or env.get(MLPSTORAGE_ORGNAME_ENVVAR)`; symmetric :602 for SYSTEMNAME; 3 new unit tests (test_args_orgname/systemname_satisfies_without_env_var + test_e101_only_when_both_args_and_env_absent) + integration test test_init_then_closed_datagen_no_env_var at test_canonical_layout_end_to_end.py:405 all PASS; docstring at :538 documents the args-first precedence + LAY-03 hook intent + env-fallback rationale."
  - id: HARDEN-04
    status: SATISFIED
    evidence: "RED 5682e60 + GREEN 362cffa + REFACTOR b8e5eea + docs a3599fe + SUMMARY 2dcef73; _strip_tag_output_prefix helper at cluster_collector.py:3313 with compiled regex _TAG_OUTPUT_PREFIX_RE at :3310 `r'^\\[[^\\]]+\\](?:<[a-z]+>:?)?\\s*'`; helper consumed by production launcher (:3584) + 4 integration test sites + 2 unit test methods; TestTagOutputRegexParsesOpenMpi4xPrefix at test_cluster_collector.py:3600 with verbatim OpenMPI 4.1.6 byte-string fixture + parametrized backward-compat test + structural-defense pytest.raises(JSONDecodeError) against old regex; 4/4 test_shared_fs_probe_real_mpi.py PASS in 21.83s on dev box with real OpenMPI 4.1.6 + mpi4py 4.1.2."
human_verification:
  - test: "HARDEN-01 end-to-end CLI repro (05.1-UAT.md Test 1)"
    expected: "./mlpstorage init <orgname> <results-dir> && ./mlpstorage closed training unet3d datagen file -rd <results-dir> -np 4 -sn <sys> -dd <data-dir> completes CAP-01 without E201 AND without E101. Either lazy-collects real cluster_information OR emits 'CAP-01 deferred: unable to determine system memory' INFO log and proceeds. Test 1's resolved_by chain (HARDEN-01 + HARDEN-03) is now codebase-complete; /gsd-verify-work should flip `result: issue` → `result: pass` after this run."
    why_human: "Unit tests + in-process integration test cover the Python contract. The CLI invocation exercises argparse + registry dispatch + Benchmark._pre_execution_gate + real accumulate_host_info, which only run end-to-end from the CLI. Requires datagen subparser host; ideally psutil/mpi4py for happy path."
  - test: "ROADMAP SC#4 — UAT Test 4 multi-host CAP-02 hardware reproduction (REVIEW-CR-02 closure)"
    expected: "Re-run 05-UAT.md Test 4 on real multi-host hardware (launch host outside --hosts). Either passes silently OR fails with accurate diagnostic naming the real cause. The misleading 'mpi4py not installed on all hosts' message stays absent (grep -c = 0 in cluster_collector.py). HARDEN-04 closure (real OpenMPI 4.x tag-output) makes this re-run actually meaningful: a hardware run on 4.1.6+ should now produce parseable rank-0 JSON, not a JSONDecodeError."
    why_human: "Requires multi-host SSH + OpenMPI + mpi4py deployment with launch host outside --hosts. Per D-58 + CONTEXT, deferred to a separate hardware-verification cycle; NOT a Phase 5.1 prerequisite. Carry-forward note already in 05-UAT.md Test 4."
  - test: "/gsd-verify-work pass to flip 05.1-UAT.md Test 1 + Test 2 result keys"
    expected: "/gsd-verify-work .planning/phases/05.1-phase-5-hardening-uat-closeout/05.1-UAT.md should now (a) for Test 1, run the init→closed-datagen command and flip `result: issue` → `result: pass` (the 6-commit fix-train is in place: 754763a + 29f1062 + d13b418 + e5b7b8b + 795a088 + 3dbc2d3); (b) for Test 2, run the localhost real-mpirun pytest and flip `result: issue` → `result: pass` (the 5-commit fix-train is in place: f55fd5d + 086b2a9 + 5682e60 + 362cffa + b8e5eea — 4/4 tests already PASS in isolation per this verifier's 21.83s run)."
    why_human: "Per project convention, /gsd-verify-work owns the UAT result-key flip after re-running the documented `command:` block. This verifier confirmed all 5+ commits per test are landed and the underlying tests pass, but does not flip UAT result keys."
---

# Phase 05.1: Phase 5 Hardening & UAT Closeout Verification Report

**Phase Goal:** Phase 5's UAT (deferred 2026-06-24) surfaced two regressions that block phase sign-off — (a) CAP-01 capacity gate AttributeError on datagen (HARDEN-01); (b) CAP-02 file-based transport breaks when rank 0 is remote (HARDEN-02). Re-opened 2026-06-24 after Phase 5.1's own UAT (Tests 1+2) surfaced two SECONDARY regressions inside the HARDEN-01/02 fix trains — (c) init→closed-datagen still raises E101 because the env-validator never consults args.orgname (HARDEN-03); (d) HARDEN-02 GREEN's tag-strip regex assumed wrong OpenMPI prefix format and JSONDecodeError-failed on real OpenMPI 4.x output (HARDEN-04). Closure of all four lands across Plans 05.1-01..04.
**Verified:** 2026-06-25T08:00:00Z
**Status:** human_needed
**Re-verification:** Yes — incremental verification of HARDEN-03 + HARDEN-04 gap closure landed on top of the 2026-06-24 initial verification (which covered HARDEN-01 + HARDEN-02). All previous 6/6 must-haves remain VERIFIED + 2 new must-haves (SC#7 + SC#8) added by ROADMAP commit `af69f59`. Score 8/8.

## Goal Achievement

### Observable Truths (ROADMAP SC#1–#8)

| #   | Truth (Success Criterion)                                                                                                                                                                                                                                                                                                                                                                              | Status                          | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | SC#1: `./mlpstorage init … && ./mlpstorage closed training unet3d datagen file …` completes the CAP-01 capacity check without raising E201                                                                                                                                                                                                                                                              | VERIFIED (unit) / HUMAN (CLI)   | `mlpstorage_py/benchmarks/dlio.py:533` has `cluster_info = getattr(self, "cluster_information", None)`; lines 537–545 graceful-degradation block present. `pytest tests/unit/test_capacity_gate.py::TestTrainingBenchmarkRequiredBytes` 6/6 PASS in this verifier's run (5.24s combined w/ silence test). CLI-level end-to-end repro routed to human verification (UAT Test 1 — fix-train now complete; flip-blocker was HARDEN-03, now closed).                                                                                                                                                                                                                                                                                                                       |
| 2   | SC#2: Sibling subclasses (CheckpointingBenchmark, KVCacheBenchmark, VectorDBBenchmark) verified structurally immune                                                                                                                                                                                                                                                                                     | VERIFIED                        | `grep -n 'cluster_information' mlpstorage_py/benchmarks/{kvcache,vectordbbench,dlio}.py`: kvcache.py:97 is a WRITER (`self.cluster_information = self._collect_cluster_information()`), not a READER in a gate path; vectordbbench.py has 0 occurrences (uses `self.args.dimension`/`num_vectors` math per SUMMARY 05.1-01). CheckpointingBenchmark lives in dlio.py and uses `LLM_SIZE_BY_RANK` constants per D-50 + 05.1-01 SUMMARY. The lazy-collect/graceful-degrade pattern at dlio.py:533–545 covers all DLIO-based subclasses transitively.                                                                                                                                                                                                                     |
| 3   | SC#3: CAP-02 probe transmits rank-0 result via a transport that works when rank 0 is on a remote host                                                                                                                                                                                                                                                                                                  | VERIFIED                        | `grep -c '__CAP02_RESULT_BEGIN__' mlpstorage_py/cluster_collector.py` = 7; `__CAP02_RESULT_END__` = 6; `--tag-output` = 4. File-based output_file plumbing deleted (per 05.1-02 SUMMARY: tempfile.mkstemp delta = −1 in CAP-02 path). Stdout transport + REAL OpenMPI 4.x parsing additionally verified by HARDEN-04 GREEN/REFACTOR: 4/4 `test_shared_fs_probe_real_mpi.py` tests PASS in 21.83s on dev box.                                                                                                                                                                                                                                                                                                                                                          |
| 4   | SC#4: UAT Test 4 re-runs against fixed launcher and either passes silently OR fails with accurate diagnostic naming the real cause                                                                                                                                                                                                                                                                     | VERIFIED (code) / HUMAN (HW)    | `grep -c 'mpi4py not installed' mlpstorage_py/cluster_collector.py` = 0 (misleading substring fully purged from launcher). FileSystemError raise path at cluster_collector.py:3525–3540 names `result.returncode` + stderr tail when markers absent. HARDEN-04 additionally guarantees that on real OpenMPI 4.x hardware the markers WILL be parsed correctly (was the silent JSONDecodeError before HARDEN-04). Hardware re-run of Test 4 deferred per D-58 to a separate hardware-verification cycle.                                                                                                                                                                                                                                                                |
| 5   | SC#5: `05-UAT.md` updated — failed E201 gap marked resolved with fixing commits; Test 4 not left dangling as `pending`                                                                                                                                                                                                                                                                                | VERIFIED                        | 05-UAT.md: `status: failed` count = 0, `status: resolved` count = 1, `resolved_by:` chain `754763a, 29f1062, d13b418, e5b7b8b` present, `## Resolutions` section appended (1 occurrence), Test 4 carry-forward note pointing to hardware-verification cycle. Tests 1–4 entries preserved verbatim (confirmed in 2026-06-24 verification snapshot).                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 6   | SC#6: No regression in Phase 5 success criteria #1–#8                                                                                                                                                                                                                                                                                                                                                  | VERIFIED                        | This verifier's targeted regression run: HARDEN-01 invariant `TestTrainingBenchmarkRequiredBytes` 6/6 PASS post-HARDEN-03/04 landing; HARDEN-02 invariant `TestSharedFsProbeNonRank0Silence` 4/4 PASS post-HARDEN-03/04 landing (10/10 combined). HARDEN-03 (code_image.py) does not touch cluster_collector.py or dlio.py. HARDEN-04 only touches cluster_collector.py tag-strip regex (no touch of dlio.py or capacity gate). Phase 5 diff-lifecycle and capacity-gate behaviors continue to hold.                                                                                                                                                                                                                                                                  |
| 7   | **SC#7 (HARDEN-03 closure):** After `mlpstorage init`, the subsequent `mlpstorage closed\|open` command does NOT require `MLPSTORAGE_ORGNAME` exported — `capture_or_verify_code_image` consults `args.orgname` (populated by LAY-03 gate from sentinel) BEFORE env fallback. Symmetric SYSTEMNAME. Full-CLI integration test proves the wiring end-to-end.                                            | VERIFIED                        | `mlpstorage_py/submission_checker/tools/code_image.py:563`: `orgname = getattr(args, "orgname", None) or env.get(MLPSTORAGE_ORGNAME_ENVVAR)` confirmed in-file (`grep -c 'getattr(args, "orgname"' = 1`); :602 symmetric for SYSTEMNAME (`grep -c 'getattr(args, "systemname"' = 1`); HARDEN-03 markers at code_image.py:538, 556, 571, 599, 609 (`grep -c 'HARDEN-03' = 5`); docstring at :538 documents args-first precedence + LAY-03 hook + env-fallback rationale. Tests: `test_args_orgname_satisfies_without_env_var` + `test_args_systemname_satisfies_without_env_var` + `test_e101_only_when_both_args_and_env_absent` at `mlpstorage_py/tests/test_capture_or_verify_code_image.py:146-180`; full-CLI integration `test_init_then_closed_datagen_no_env_var` at `tests/integration/test_canonical_layout_end_to_end.py:405`. All 38/38 in the HARDEN-03 suite PASS in this verifier's run.   |
| 8   | **SC#8 (HARDEN-04 closure):** Under real OpenMPI 4.x `mpirun --tag-output --host 127.0.0.1:1,127.0.0.1:1 -n 2`, the tag-strip regex correctly consumes BOTH `[rank,jobid]` bracketed identifier AND optional `<channel>:` marker. All 4 integration tests in `test_shared_fs_probe_real_mpi.py` PASS GREEN; production launcher and 4 test sites consume shared `_strip_tag_output_prefix` helper. | VERIFIED                        | `mlpstorage_py/cluster_collector.py:3310` compiled regex `_TAG_OUTPUT_PREFIX_RE = re.compile(r"^\[[^\]]+\](?:<[a-z]+>:?)?\s*")`; :3313 `def _strip_tag_output_prefix(line: str)`; :3584 launcher consumes helper via `_payload = _strip_tag_output_prefix(_payload_raw)`. Test sites: `tests/integration/test_shared_fs_probe_real_mpi.py:138,206,280,323` (4 sites — `grep -c '_strip_tag_output_prefix' = 5` including import). Unit tests: `TestTagOutputRegexParsesOpenMpi4xPrefix` at `tests/unit/test_cluster_collector.py:3600` — `test_openmpi_4x_tag_output_format_parses` + parametrized `test_tag_strip_handles_both_formats` (openmpi_4x + legacy_host_rank). **4/4 `test_shared_fs_probe_real_mpi.py` PASS in 21.83s** on dev box with real OpenMPI 4.1.6 + mpi4py 4.1.2 (pre-HARDEN-04 all 4 FAILED RED with JSONDecodeError per the debug session and 05.1-04 SUMMARY).            |

**Score:** 8/8 must-haves VERIFIED at the codebase/unit-test/real-mpirun-integration-test level. 3 items additionally routed to human verification for end-to-end CLI runtime evidence + hardware re-run of UAT Test 4 + /gsd-verify-work UAT result-key flip — all are the standard runtime/hardware defer pattern, not Phase 5.1 regressions.

### Required Artifacts

| Artifact                                                                                       | Expected                                                                                                                                                            | Status                          | Details                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `mlpstorage_py/benchmarks/dlio.py`                                                             | HARDEN-01 lazy-collect + graceful-degradation block at lines 533–545; unchanged by Plans 03/04 (cross-plan invariant)                                                | VERIFIED                        | Line 533 `cluster_info = getattr(self, "cluster_information", None)`; lines 537–545 INFO log + `return 0`. Neither Plan 03 (code_image.py) nor Plan 04 (cluster_collector.py) touched dlio.py.                                                                                                                                                                                                                       |
| `tests/unit/test_capacity_gate.py`                                                             | HARDEN-01 regression tests + traceability comment (locked cross-plan)                                                                                               | VERIFIED                        | line 350 `"""HARDEN-01 regression guard: locks the 754763a + 29f1062 fix in place (Phase 5.1)."""`; 6/6 `TestTrainingBenchmarkRequiredBytes` PASS post-HARDEN-03/04.                                                                                                                                                                                                                                                 |
| `mlpstorage_py/cluster_collector.py`                                                           | HARDEN-02 stdout-marker transport + HARDEN-04 `_strip_tag_output_prefix` helper + `_TAG_OUTPUT_PREFIX_RE` consuming `[rank,jobid]<channel>:`                         | VERIFIED                        | HARDEN-02 markers: BEGIN×7, END×6, `--tag-output`×4. Misleading `'mpi4py not installed'`×0. HARDEN-04 helper at :3313 with regex at :3310; launcher consumes at :3584. `grep -c '_strip_tag_output_prefix' = 4` (1 def + 3 refs); `grep -c 'HARDEN-04' = 6`. Helper docstring documents OpenMPI 4.x format + debug-session reference.                                                                                  |
| `tests/unit/test_cluster_collector.py`                                                         | HARDEN-02 `TestSharedFsProbeNonRank0Silence` + HARDEN-04 `TestTagOutputRegexParsesOpenMpi4xPrefix` w/ verbatim OpenMPI 4.1.6 byte-string fixture + structural defense | VERIFIED                        | line 3487 `class TestSharedFsProbeNonRank0Silence` (4/4 PASS post-HARDEN-04); line 3600 `class TestTagOutputRegexParsesOpenMpi4xPrefix`; :3623 `_OPENMPI_4X_STDOUT_FIXTURE` verbatim bytes; `test_openmpi_4x_tag_output_format_parses` at :3640; parametrized `test_tag_strip_handles_both_formats` at :3680. Helper imported at line 23. Structural-defense `pytest.raises(json.JSONDecodeError)` on OLD regex preserved post-REFACTOR (locks the regression shape independent of the helper). |
| `tests/integration/test_shared_fs_probe_real_mpi.py`                                           | All 4 tests use shared `_strip_tag_output_prefix` helper (no inline regex); all 4 PASS GREEN against real OpenMPI 4.1.6 + mpi4py 4.1.2                              | VERIFIED                        | Helper imported at line 57; 4 consumption sites at lines 138, 206, 280, 323 (`grep -c '_strip_tag_output_prefix' = 5`). **All 4 tests PASS in 21.83s in this verifier's isolated run**: `test_d49_quiesce_observable_via_wall_clock`, `test_cr02_rank0_result_arrives_via_stdout_markers_not_file`, `test_two_local_ranks_outputs_carry_correct_uuid`, plus the 4th in `TestSharedFsProbeRealMpi` (parametrized).                                                            |
| `mlpstorage_py/submission_checker/tools/code_image.py`                                         | HARDEN-03 `args.orgname` → env fallback at :563; symmetric SYSTEMNAME at :602; refreshed suggestion text; docstring documenting args-first precedence                | VERIFIED                        | Line 563 `orgname = getattr(args, "orgname", None) or env.get(MLPSTORAGE_ORGNAME_ENVVAR)`; line 602 `systemname = getattr(args, "systemname", None) or env.get(MLPSTORAGE_SYSTEMNAME_ENVVAR)`; docstring at :538 explicitly documents args-first precedence + LAY-03 hook + env-fallback rationale. Stale `# future: mlpstorage init` comment refreshed (per 05.1-03 SUMMARY grep gate `grep -v '^#' \| grep -c '# future:' = 0`). `grep -c 'HARDEN-03' = 5`.                                          |
| `mlpstorage_py/tests/test_capture_or_verify_code_image.py`                                     | 3 new HARDEN-03 unit tests + `_make_args` fixture extended w/ `orgname=` param                                                                                       | VERIFIED                        | `_make_args` at :67 with `orgname=None` kwarg (forwarded to args at :74 with HARDEN-03 comment); `test_args_orgname_satisfies_without_env_var` at :146; `test_args_systemname_satisfies_without_env_var` at :156; `test_e101_only_when_both_args_and_env_absent` at :165. All 3 PASS in this verifier's run (32/32 total in the file).                                                                                |
| `tests/integration/test_canonical_layout_end_to_end.py`                                        | New `TestInitThenRunFullCliDispatch::test_init_then_closed_datagen_no_env_var` drives `_main_impl` in-process, DependencyError allowlisted as valid post-gate exit  | VERIFIED                        | Class at :393, test at :405. Comment block at :389 documents HARDEN-03 scope. `DependencyError` import at :486 (`from mlpstorage_py.errors import ConfigurationError, DependencyError`); allowlist branch at :501 with rationale "got PAST the env-var gate (which is the only contract under test)". 6/6 tests in the file PASS in this verifier's run.                                                                |
| `.planning/phases/05.1-phase-5-hardening-uat-closeout/05.1-UAT.md`                             | Test 1 + Test 2 entries with `resolved_by:` chains extended w/ HARDEN-03/04 SHAs + `harden_03_addendum:` + `harden_04_addendum:` keys; Summary totals coherent       | VERIFIED                        | Test 1 (:32 `result: issue`; :53 `resolved_by:` chain `754763a, 29f1062, d13b418, e5b7b8b, 795a088, 3dbc2d3`; :57 `harden_03_addendum:` block documenting init↔env-validator gap + HARDEN-03 RED/GREEN closure). Test 2 (:82 `result: issue`; :106 `resolved_by:` `f55fd5d, 086b2a9, 5682e60, 362cffa, b8e5eea`; :111 `harden_04_addendum:` block documenting OpenMPI 4.x format root cause + RED/GREEN/REFACTOR closure). Summary at :130–133: `total: 2, passed: 0, issues: 2, pending: 0`.                          |

### Key Link Verification

| From                                                                                                                | To                                                                                              | Via                                                                                                                                                  | Status     | Details                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `main._main_impl` LAY-03 gate (main.py:356–389)                                                                     | `capture_or_verify_code_image` at code_image.py:563                                              | `args.orgname` populated from `mlperf-results.yaml` sentinel via `resolve_orgname(results_dir_value)`; helper consumes via `getattr(args, "orgname")` | WIRED      | Verified via integration test `test_init_then_closed_datagen_no_env_var` at `test_canonical_layout_end_to_end.py:405` — drives full `_main_impl` in-process with `MLPSTORAGE_ORGNAME` UNSET, asserts no E101 and `capture_or_verify_invoked` flag set post-gate. Test PASSES. |
| `_TAG_OUTPUT_PREFIX_RE` at cluster_collector.py:3310                                                                | Production launcher at :3584 + 4 integration test sites + 2 unit test methods                  | `_strip_tag_output_prefix(line)` helper at :3313                                                                                                     | WIRED      | `grep -c '_strip_tag_output_prefix' mlpstorage_py/cluster_collector.py = 4`; `tests/integration/...real_mpi.py = 5`; `tests/unit/test_cluster_collector.py = 3`. Single source of truth confirmed — no duplicated inline `re.sub(r'^\[[^\]]+\]...` regex remains.                                              |
| `test_openmpi_4x_tag_output_format_parses`                                                                          | Production helper + structural defense against OLD regex                                        | `_strip_tag_output_prefix(payload_raw)` + inline `re.sub(r"^\[[^\]]+\]\s*", ...)` + `pytest.raises(json.JSONDecodeError)`                            | WIRED      | Two-sided lock: (1) helper produces valid JSON → `json.loads` succeeds; (2) OLD broken pattern inlined produces invalid JSON → `JSONDecodeError`. A future regression that reverted the helper would fail (1) and re-prove the regression shape via (2).                                                       |
| HARDEN-03 RED commit `795a088`                                                                                       | Test fixtures `_make_args` extension + 3 new tests + integration test                          | RED commit ADDS the failing tests; GREEN commit `3dbc2d3` FLIPS them by changing code_image.py:563/602                                              | WIRED      | RED commit body documents the pre-fix failure: "30 passed, 2 failed (the two new args-first tests)"; GREEN commit body documents the flip. Verified via `git show 795a088 --stat` showing only test files; `git show 3dbc2d3 --stat` showing code_image.py + integration test fix-up.                            |
| HARDEN-04 RED commit `5682e60`                                                                                       | Test fixture `_OPENMPI_4X_STDOUT_FIXTURE` + unit test class + production-source comment-only edits | RED commit ADDS failing unit tests + production-comment doc edits; GREEN `362cffa` flips regex inline at 5 sites; REFACTOR `b8e5eea` consolidates into helper | WIRED      | RED commit body: "pre-fix: all 4 integration tests + new unit tests FAIL RED w/ JSONDecodeError". GREEN commit body: "274/274 unit + 4/4 integration PASS post-fix". REFACTOR commit body: "single source of truth — production launcher + 4 integration tests + unit test class all consume `_strip_tag_output_prefix`". |

### Data-Flow Trace (Level 4)

| Artifact                                            | Data Variable                                                              | Source                                                                                          | Produces Real Data | Status   |
| --------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------ | -------- |
| `capture_or_verify_code_image` (orgname code path)   | `orgname` (str)                                                            | `getattr(args, "orgname", None) or env.get(MLPSTORAGE_ORGNAME_ENVVAR)`                          | YES (args from LAY-03 gate, populated from `mlperf-results.yaml` sentinel via `resolve_orgname()`; env fallback for legacy callers) | FLOWING |
| `capture_or_verify_code_image` (systemname OPEN path)| `systemname` (str)                                                         | `getattr(args, "systemname", None) or env.get(MLPSTORAGE_SYSTEMNAME_ENVVAR)`                    | YES (args from CLI `--systemname`; env fallback for legacy callers; future sentinel-field hook ready) | FLOWING |
| `run_shared_fs_probe` launcher (rank-0 payload parse)| `_payload` (str)                                                           | `_strip_tag_output_prefix(_m.group("payload").strip())` → `json.loads(_payload)`                 | YES (real mpirun subprocess.run stdout → marker regex extract → tag-strip helper → JSON dict with `status: "ok"` + per-rank entries)                       | FLOWING |

### Behavioral Spot-Checks

| Behavior                                                                                          | Command                                                                                                                                              | Result                                                                                                                                                   | Status |
| ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| HARDEN-01/03/04 combined targeted suite passes                                                    | `pytest mlpstorage_py/tests/test_capture_or_verify_code_image.py tests/integration/test_canonical_layout_end_to_end.py tests/unit/test_capacity_gate.py tests/unit/test_cluster_collector.py -v` | **344 passed in 9.41s**                                                                                                                                  | PASS   |
| HARDEN-01 invariant preserved post-HARDEN-03/04                                                   | `pytest tests/unit/test_capacity_gate.py::TestTrainingBenchmarkRequiredBytes -v`                                                                     | 6/6 PASS (within 10/10 combined run)                                                                                                                     | PASS   |
| HARDEN-02 silence invariant preserved post-HARDEN-03/04                                           | `pytest tests/unit/test_cluster_collector.py::TestSharedFsProbeNonRank0Silence -v`                                                                   | 4/4 PASS (within 10/10 combined run)                                                                                                                     | PASS   |
| HARDEN-04 real-OpenMPI-4.x integration suite passes in isolation                                  | `pytest tests/integration/test_shared_fs_probe_real_mpi.py -v`                                                                                       | **4 passed in 21.83s** on dev box (OpenMPI 4.1.6 + mpi4py 4.1.2)                                                                                         | PASS   |
| HARDEN-02 misleading mpi4py substring purged from launcher                                        | `grep -c 'mpi4py not installed' mlpstorage_py/cluster_collector.py`                                                                                  | 0                                                                                                                                                        | PASS   |
| HARDEN-04 single source of truth — helper consumed across production + tests                      | `grep -c '_strip_tag_output_prefix' mlpstorage_py/cluster_collector.py tests/integration/test_shared_fs_probe_real_mpi.py tests/unit/test_cluster_collector.py` | 4 + 5 + 3 = 12 references; one `def` at cluster_collector.py:3313 (single source of truth)                                                               | PASS   |
| HARDEN-03 args-first precedence visible in code_image.py                                          | `grep -c 'getattr(args, "orgname"' mlpstorage_py/submission_checker/tools/code_image.py` + `getattr(args, "systemname"`                              | 1 + 1 = 2 (matches plan grep gates)                                                                                                                      | PASS   |
| HARDEN-03 stale `# future:` suggestion removed                                                    | `grep -v '^#' mlpstorage_py/submission_checker/tools/code_image.py \| grep -c '# future: mlpstorage init'`                                           | 0                                                                                                                                                        | PASS   |
| RED→GREEN ordering for HARDEN-03                                                                  | `git log af69f59..HEAD --reverse --format='%h %s' \| head -3`                                                                                        | `795a088 test(...): RED` → `3dbc2d3 fix(...): GREEN` → `993bd11 docs(...)`                                                                              | PASS   |
| RED→GREEN→REFACTOR ordering for HARDEN-04                                                         | `git log af69f59..HEAD --reverse --format='%h %s' \| sed -n '5,8p'`                                                                                  | `5682e60 test(...): RED` → `362cffa fix(...): GREEN` → `b8e5eea refactor(...)` → `a3599fe docs(...)`                                                    | PASS   |
| No Claude attribution in any commit since phase re-open                                           | `git log af69f59..HEAD --format=%B \| grep -ic claude`                                                                                                | 0                                                                                                                                                        | PASS   |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| n/a — Phase 5.1 declares no `scripts/*/tests/probe-*.sh` shell-probe entry points |  |  | SKIPPED (no probes) |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                                                | Status    | Evidence                                                                                                                                                                                                                                                                          |
| ----------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HARDEN-01   | 05.1-01-PLAN.md | CAP-01 capacity check completes on datagen path without AttributeError                                                                                  | SATISFIED | Lazy-collect + graceful-degradation at dlio.py:533–545; 6/6 regression tests PASS post-HARDEN-03/04 (cross-plan invariant preserved).                                                                                                                                                |
| HARDEN-02   | 05.1-02-PLAN.md | CAP-02 probe transmits rank-0 result via stdout markers; launcher never raises misleading mpi4py error on probe semantic success                         | SATISFIED | Stdout markers + --tag-output landed; misleading substring purged; 4/4 silence test PASS; file-based transport deleted in same commit (D-57).                                                                                                                                       |
| HARDEN-03   | 05.1-03-PLAN.md | `capture_or_verify_code_image` consults `args.orgname` (populated by LAY-03 gate from sentinel) before env fallback; symmetric SYSTEMNAME              | SATISFIED | code_image.py:563/602 args-first precedence; 3 new unit tests + integration test PASS; docstring documents intent + LAY-03 hook + env-fallback rationale.                                                                                                                              |
| HARDEN-04   | 05.1-04-PLAN.md | Tag-strip regex consumes OpenMPI 4.x `[rank,jobid]<channel>:` prefix; production launcher + 4 test sites consume shared `_strip_tag_output_prefix` helper | SATISFIED | Helper at cluster_collector.py:3313 (single source of truth); 4/4 real-OpenMPI-4.1.6 integration tests PASS in 21.83s; structural defense locks regression shape via `pytest.raises(JSONDecodeError)` on OLD pattern.                                                                |

**Orphaned requirements check:** REQUIREMENTS.md Traceability table maps HARDEN-01..04 → Phase 5.1; all four appear in plan frontmatter `requirements:` fields. No orphans.

### Anti-Patterns Found

| File                                                                                                  | Line  | Pattern                                                                                                                | Severity | Impact                                                                                                                |
| ----------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------- |
| (none)                                                                                                |       | No `TBD`/`FIXME`/`XXX` debt markers in any Phase 5.1 modified file (code_image.py, cluster_collector.py, test files). |          | All production code paths exercised by tests; no debt markers introduced; HARDEN-03 stale `# future:` comment removed. |

Existing pre-Phase 5.1 advisory items (NOT 5.1-introduced; NOT blockers — carried forward from the 2026-06-24 initial verification):
- **REVIEW-CR-01** (shell-injection in CAP-02 launcher): out of 5.1 scope per CONTEXT D-50..D-59; advisory.
- **WR-01..WR-04** in 05.1-REVIEW.md: 0 critical findings flagged by reviewer; advisory.
- **~70 pre-existing unit-suite failures** in `test_datagen_command_generation.py`, `test_rules_calculations.py`, `test_benchmarks_base.py`, `test_generate_output_location.py`, `test_open_closed_flag_recognition.py`: documented as PRE-EXISTING in 05.1-02 / 05.1-03 / 05.1-04 SUMMARY files — LAY-03 fixture-migration debt + typer/MagicMock + numpy/psutil import issues that pre-date Phase 5.1. NOT Phase 5.1 regressions.
- **Pre-existing integration test-isolation pollution** at `test_shared_fs_probe_real_mpi.py:53–55` (`sys.modules['pyarrow'] = MagicMock()`): the 4 real-mpirun tests PASS in isolation but fail with empty stdout when run with the rest of the integration suite (real `pyarrow` import collision with the MagicMock injection). Documented in 05.1-04 SUMMARY as a deferred issue; NOT caused by HARDEN-04 — same issue would have existed under HARDEN-02 GREEN but was masked by mpi4py being absent.

### Human Verification Required

3 items deferred to human / hardware / `/gsd-verify-work` cycle — all match the established defer pattern, none are Phase 5.1 regressions:

#### 1. HARDEN-01 end-to-end CLI repro (05.1-UAT.md Test 1)

**Test:** `./mlpstorage init <orgname> <results-dir> && ./mlpstorage closed training unet3d datagen file -rd <results-dir> -np 4 -sn <sys> -dd <data-dir>`
**Expected:** CAP-01 capacity check completes without `AttributeError: 'TrainingBenchmark' object has no attribute 'cluster_information'` AND without `[E101] MLPSTORAGE_ORGNAME environment variable is required`. Either lazy-collects real cluster_information (happy path) OR emits one `"CAP-01 deferred: unable to determine system memory ..."` INFO log and proceeds into actual data generation. Fix-train now codebase-complete (HARDEN-01 + HARDEN-03 both landed): `754763a, 29f1062, d13b418, e5b7b8b, 795a088, 3dbc2d3`.
**Why human:** Unit + in-process integration tests cover the Python contract. The CLI invocation exercises argparse + registry dispatch + `Benchmark._pre_execution_gate` + real `accumulate_host_info`, which only run end-to-end from the CLI.

#### 2. ROADMAP SC#4 — UAT Test 4 multi-host CAP-02 hardware reproduction (REVIEW-CR-02 closure)

**Test:** Launch CAP-02 from a host NOT in `--hosts` against a real multi-host shared-FS deployment (e.g., laptop + 2 physical client nodes), running OpenMPI 4.x.
**Expected:** Either the bug is observed as a real failure with an accurate diagnostic that names the actual cause (returncode + stderr tail) — the misleading `"mpi4py not installed on all hosts"` substring is NOT present (verified `grep -c = 0` in launcher) — OR the probe succeeds silently on the happy path. HARDEN-04 additionally guarantees that the tag-output stdout transport parses correctly on real OpenMPI 4.x hardware (was silently broken pre-HARDEN-04).
**Why human:** Requires multi-host SSH + OpenMPI + mpi4py deployment with launch host outside `--hosts`. Per D-58 + CONTEXT explicit scope decision, deferred to a separate hardware-verification cycle and NOT a Phase 5.1 prerequisite.

#### 3. `/gsd-verify-work` pass to flip 05.1-UAT.md Test 1 + Test 2 `result:` keys

**Test:** `/gsd-verify-work .planning/phases/05.1-phase-5-hardening-uat-closeout/05.1-UAT.md`
**Expected:** Both Test 1 and Test 2 transition from `result: issue` to `result: pass` after re-running the documented `command:` blocks. Test 1 fix-train (6 commits) and Test 2 fix-train (5 commits) are both codebase-complete and the underlying tests already PASS (Test 2's localhost-mpirun pytest PASSES 4/4 in 21.83s per this verifier's run).
**Why human:** Per project convention, `/gsd-verify-work` owns UAT `result:` key flips after re-running documented `command:` blocks. This verifier confirms all fix-train commits landed and underlying tests pass, but does not modify UAT result keys.

### Gaps Summary

None. The phase goal — "both regressions are fixed across all affected benchmark subclasses; UAT Test 4 can be re-run on real hardware; the two secondary regressions inside the HARDEN-01/02 fix trains (HARDEN-03 + HARDEN-04) are also closed" — is achieved in code:

- **HARDEN-01 (CAP-01 E201):** lazy-collect + graceful-degradation at `dlio.py:533–545`; regression tests lock it; cross-plan invariant preserved post-HARDEN-03/04 landing.
- **HARDEN-02 (CAP-02 file-transport):** stdout-marker transport landed; file-based code deleted; silence test locks rank-0-only contract; misleading mpi4py substring purged.
- **HARDEN-03 (init→env-validator gap):** `code_image.py:563/602` consults `args.orgname` / `args.systemname` BEFORE env fallback; LAY-03 gate's sentinel-populated args now flow into the submission-checker validator end-to-end; 4 new tests (3 unit + 1 integration) lock it.
- **HARDEN-04 (OpenMPI 4.x tag-output regex):** `_strip_tag_output_prefix` helper at `cluster_collector.py:3313` consumes `[rank,jobid]<channel>:` prefix correctly; single source of truth across production launcher + 4 integration tests + unit test class; 4/4 real-OpenMPI-4.1.6 integration tests PASS GREEN in 21.83s.
- **UAT Test 4 (multi-host hardware):** explicitly deferred per D-58; not a 5.1 prerequisite; the code changes required to make Test 4 "re-runnable with accurate diagnostic instead of misleading one" + "real-OpenMPI-4.x stdout transport works end-to-end" have BOTH landed.

The status is `human_needed` (not `passed`) solely because three runtime/hardware items genuinely require human verification per the established defer pattern (UAT Test 1 CLI repro + UAT Test 4 hardware re-run + `/gsd-verify-work` UAT result-key flip) — not because the codebase-level goal is unachieved.

### Process / Plan Notes (informational; no blockers)

- **Plan 05.1-03 transient `git stash` use (recovered immediately):** SUMMARY 05.1-03 §"Process deviation" documents an isolated `git stash push` for state isolation that was immediately `git stash pop`'d with no sibling worktree active and no cross-contamination. Going-forward use of `git show <ref>:<path>` for read-only inspection per sanctioned alternatives. NOT a blocker — recovery confirmed clean.
- **Plan 05.1-04 deferred test-isolation pollution:** the 4 real-mpirun integration tests in `test_shared_fs_probe_real_mpi.py` pass in isolation (21.83s) but fail with empty stdout when run alongside other integration tests due to `sys.modules['pyarrow'] = MagicMock()` injection at file-level. Documented in 05.1-04 SUMMARY as pre-existing test-fixture scoping issue, would have surfaced under HARDEN-02 GREEN if mpi4py had been available then. NOT a Phase 5.1 regression; recommend a future plan to provide proper fixture-level scoping for the MagicMock subprocess-fork pollution.

---

## Appendix: Original 2026-06-24 Verification (HARDEN-01 + HARDEN-02 initial scope)

The historical content of the 2026-06-24 initial verification — which scored 6/6 must-haves VERIFIED for the original SC#1–#6 with status `human_needed` — is preserved here for audit-trail continuity. Re-verification on 2026-06-25 confirms all 15 truths from that initial verification remain VERIFIED post-HARDEN-03/04 landing (HARDEN-01 invariant: 6/6 `TestTrainingBenchmarkRequiredBytes` PASS; HARDEN-02 invariant: 4/4 `TestSharedFsProbeNonRank0Silence` PASS; misleading-mpi4py-substring purge: `grep -c = 0`).

### Initial-verification metadata
- **verified:** 2026-06-24T00:00:00Z
- **status:** human_needed
- **score:** 6/6 must-haves verified
- **overrides_applied:** 0

### Initial-verification truth table (SC#1–#6 + 9 plan-level truths)

The initial verification documented 15 truths VERIFIED — SC#1–#6 plus PLAN-01 + PLAN-02 internal must-haves (RED+GREEN commit pairs with the correct scopes, HARDEN-01 traceability comment grep-able from test file, non-rank-0 silence test parametrized over ranks [0,1,2,3], D-57 deletion-in-same-plan, localhost real-mpirun integration test exists in code, 05.1-UAT.md Test 1+2 entries present, D-50 scope fences honored, no Claude attribution).

Cross-plan regression check on 2026-06-25 (this re-verification): all 15 truths still hold. Specifically:
- dlio.py byte-identical to its pre-Plan-01 state (`git diff bbac6bf -- mlpstorage_py/benchmarks/dlio.py | wc -l` = 0) — neither HARDEN-03 nor HARDEN-04 touched dlio.py.
- cluster_collector.py SHARED_FS_PROBE_SCRIPT marker emission untouched by HARDEN-04 (HARDEN-04 only added `_TAG_OUTPUT_PREFIX_RE` + `_strip_tag_output_prefix` helper and changed the launcher's payload consumption from inline `re.sub` to helper call).
- 05-UAT.md Resolutions section / status:resolved / resolved_by chain preserved verbatim.

### Initial-verification human-verification items (status as of 2026-06-25)

The three human-verification items from 2026-06-24 (HARDEN-02 localhost real-mpirun test under mpi4py, HARDEN-01 CLI repro, UAT Test 4 hardware re-run) have been UPDATED in this 2026-06-25 re-verification's human_verification list:
1. **HARDEN-02 localhost real-mpirun test under mpi4py:** RESOLVED in this verifier's run — `pytest tests/integration/test_shared_fs_probe_real_mpi.py -v` = 4/4 PASS in 21.83s on dev box with OpenMPI 4.1.6 + mpi4py 4.1.2 (mpi4py 4.1.2 is now installed in .venv per HARDEN-04 GREEN test-run; HARDEN-04 closure additionally fixed the JSONDecodeError that would have happened anyway under the previous regex). No longer requires human; the structural test contract is now codebase-verified.
2. **HARDEN-01 CLI repro:** STILL DEFERRED to human (re-listed in the 2026-06-25 human_verification table — the unit-level + in-process-integration-level contracts are now both codebase-verified for HARDEN-01 + HARDEN-03; only the actual CLI subprocess invocation requires human confirmation).
3. **UAT Test 4 multi-host hardware reproduction:** STILL DEFERRED per D-58 (re-listed in the 2026-06-25 human_verification table; HARDEN-04 additionally guarantees the tag-output stdout transport will now parse correctly on the hardware).

The initial-verification appendix is preserved for audit-trail continuity. The primary current verification is the 2026-06-25 8/8 must-haves VERIFIED content above.

---

_Initial verification: 2026-06-24 (HARDEN-01 + HARDEN-02 scope, 6/6 must-haves)_
_Re-verification: 2026-06-25T08:00:00Z (HARDEN-01..04 scope, 8/8 must-haves)_
_Verifier: Claude (gsd-verifier)_
